// Extract artifacts (e.g. CSS) from processors

// collect all artifacts and run each rule through stylis to support nesting
const cssText = ".c19s9572{background-color:pink;color:red;font-size:3rem;}\n";

// the sourcemap will also be generated in this action
